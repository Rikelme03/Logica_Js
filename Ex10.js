// Contar Ocorrências de Palavras

// Peça uma frase e retorne a palavra que mais se repete. Use split(), reduce() e um
// objeto como contador.

var prompt = require('prompt-sync')();

let resposta = prompt("Digite uma frase: ");

function repete(texto) {
    let palavras = texto.toLo
}

